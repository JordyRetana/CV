<Window x:Class="CVDesktopEditor.AiTailorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ajustar con IA"
        Height="680"
        Width="980"
        MinHeight="620"
        MinWidth="900"
        Icon="/Assets/appicon.ico"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource AppBgBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="220"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Ajustar CV con IA"
                               FontSize="30"
                               FontWeight="Bold"/>
                    <TextBlock Text="Pega una descripcion de empleo y la IA ajustara resumen, proyectos y habilidades."
                               Margin="0,8,0,0"
                               Foreground="{StaticResource MutedBrush}"
                               TextWrapping="Wrap"/>
                </StackPanel>
                <ComboBox Name="CmbLanguage"
                          Grid.Column="1"
                          Height="42"
                          VerticalAlignment="Center">
                    <ComboBoxItem Content="CV Español" Tag="es" IsSelected="True"/>
                    <ComboBoxItem Content="CV Inglés" Tag="en"/>
                </ComboBox>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrushSoft}"
                    BorderThickness="1"
                    CornerRadius="18"
                    Padding="22">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Descripcion del empleo"
                               FontSize="20"
                               FontWeight="SemiBold"/>
                    <TextBox Name="TxtJobDescription"
                             Grid.Row="2"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             VerticalContentAlignment="Top"/>
                </Grid>
            </Border>

            <Border Grid.Column="2"
                    Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrushSoft}"
                    BorderThickness="1"
                    CornerRadius="18"
                    Padding="22">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Resultado IA"
                               FontSize="20"
                               FontWeight="SemiBold"/>
                    <TextBox Name="TxtPreview"
                             Grid.Row="2"
                             AcceptsReturn="True"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             VerticalContentAlignment="Top"/>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="4"
                   Name="TxtStatus"
                   Foreground="{StaticResource MutedBrush}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cerrar"
                    Width="130"
                    Height="44"
                    Margin="0,0,12,0"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="BtnClose_Click"/>
            <Button Name="BtnApply"
                    Content="Aplicar al CV"
                    Width="150"
                    Height="44"
                    Margin="0,0,12,0"
                    Style="{StaticResource SecondaryButtonStyle}"
                    IsEnabled="False"
                    Click="BtnApply_Click"/>
            <Button Name="BtnGenerate"
                    Content="Generar ajuste"
                    Width="160"
                    Height="44"
                    Click="BtnGenerate_Click"/>
        </StackPanel>
    </Grid>
</Window>
