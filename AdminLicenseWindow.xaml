<Window x:Class="CVDesktopEditor.AdminLicenseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
        Title="Panel admin"
        Height="680"
        Width="980"
        MinHeight="620"
        MinWidth="900"
        Icon="/Assets/appicon.ico"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource AppBgBrush}">
    <Window.Resources>
        <Style TargetType="{x:Type primitives:DatePickerTextBox}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="#0D1424"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type primitives:DatePickerTextBox}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AdminCalendarDayButtonStyle" TargetType="{x:Type CalendarDayButton}">
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="MinWidth" Value="34"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CalendarDayButton}">
                        <Border x:Name="DayChrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="1"
                                CornerRadius="5"
                                Margin="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="DayChrome" Property="Background" Value="#DBEAFE"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="DayChrome" Property="Background" Value="#2563EB"/>
                                <Setter TargetName="DayChrome" Property="BorderBrush" Value="#60A5FA"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsToday" Value="True">
                                <Setter TargetName="DayChrome" Property="BorderBrush" Value="#D6B36A"/>
                            </Trigger>
                            <Trigger Property="IsInactive" Value="True">
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                            </Trigger>
                            <Trigger Property="IsBlackedOut" Value="True">
                                <Setter Property="Foreground" Value="#CBD5E1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AdminCalendarButtonStyle" TargetType="{x:Type CalendarButton}">
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CalendarButton}">
                        <Border x:Name="MonthChrome"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="8,5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MonthChrome" Property="Background" Value="#DBEAFE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AdminCalendarStyle" TargetType="{x:Type Calendar}">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="#0D1424"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CalendarDayButtonStyle" Value="{StaticResource AdminCalendarDayButtonStyle}"/>
            <Setter Property="CalendarButtonStyle" Value="{StaticResource AdminCalendarButtonStyle}"/>
        </Style>

        <Style x:Key="AdminDatePickerStyle" TargetType="{x:Type DatePicker}">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="#0D1424"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="CalendarStyle" Value="{StaticResource AdminCalendarStyle}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="18"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel>
                    <Border Width="74"
                            Height="74"
                            HorizontalAlignment="Left"
                            Background="{StaticResource PanelBrush}"
                            BorderBrush="{StaticResource BorderBrushSoft}"
                            BorderThickness="1"
                            CornerRadius="18"
                            Padding="12">
                        <Image Source="/Assets/appicon.ico"
                               Stretch="Uniform"/>
                    </Border>

                    <TextBlock Text="Admin"
                               FontSize="34"
                               FontWeight="Bold"
                               Margin="0,24,0,0"/>
                    <TextBlock Text="Licencias"
                               Foreground="{StaticResource AccentBrush}"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Margin="0,4,0,0"/>
                    <TextBlock Text="Crea claves premium, prepara instaladores y copia mensajes listos para tus clientes."
                               Foreground="{StaticResource MutedBrush}"
                               TextWrapping="Wrap"
                               Margin="0,18,0,0"
                               LineHeight="22"/>
                </StackPanel>

                <Border Grid.Row="1"
                        Background="{StaticResource PanelBrush}"
                        BorderBrush="{StaticResource BorderBrushSoft}"
                        BorderThickness="1"
                        CornerRadius="14"
                        Padding="16"
                        VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="Entrega"
                                   Foreground="{StaticResource AccentBrush}"
                                   FontWeight="SemiBold"/>
                        <TextBlock Text="Instalador + licencia"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Margin="0,8,0,0"/>
                        <TextBlock Text="El cliente recibe el instalador normal y una clave premium. El panel admin solo aparece en tu build."
                                   Foreground="{StaticResource MutedBrush}"
                                   TextWrapping="Wrap"
                                   Margin="0,8,0,0"
                                   LineHeight="21"/>
                    </StackPanel>
                </Border>

                <TextBlock Grid.Row="2"
                           Text="No compartas tu clave admin."
                           Foreground="{StaticResource MutedBrush}"
                           TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="18"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="18"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrushSoft}"
                    BorderThickness="1"
                    CornerRadius="18"
                    Padding="22">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="14"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="14"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.ColumnSpan="3">
                        <TextBlock Text="Clave admin"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>
                        <PasswordBox Name="TxtAdminKey"
                                     Height="44"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2">
                        <TextBlock Text="Correo del cliente"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>
                        <TextBox Name="TxtEmail"
                                 Height="44"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2"
                                Grid.Column="2">
                        <TextBlock Text="Nombre del cliente"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>
                        <TextBox Name="TxtFullName"
                                 Height="44"/>
                    </StackPanel>

                    <StackPanel Grid.Row="4">
                        <TextBlock Text="Dispositivos"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>
                        <TextBox Name="TxtMaxDevices"
                                 Text="1"
                                 Height="44"/>
                    </StackPanel>

                    <StackPanel Grid.Row="4"
                                Grid.Column="2">
                        <TextBlock Text="Fecha de expiracion"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>
                        <DatePicker Name="DateExpires"
                                    Style="{StaticResource AdminDatePickerStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="2"
                    Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrushSoft}"
                    BorderThickness="1"
                    CornerRadius="18"
                    Padding="22">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DockPanel>
                        <TextBlock Text="Resultado"
                                   FontSize="22"
                                   FontWeight="SemiBold"
                                   DockPanel.Dock="Left"/>
                        <TextBlock Name="TxtServerStatus"
                                   Text="Listo"
                                   Foreground="{StaticResource AccentBrush}"
                                   HorizontalAlignment="Right"/>
                    </DockPanel>

                    <Border Grid.Row="2"
                            Background="#0D1424"
                            BorderBrush="{StaticResource BorderBrushSoft}"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="16">
                        <TextBox Name="TxtResult"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 IsReadOnly="True"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalScrollBarVisibility="Auto"/>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="1.25*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Crear instalador"
                        Height="46"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="BtnBuildInstaller_Click"/>
                <Button Grid.Column="2"
                        Content="Copiar clave"
                        Height="46"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="BtnCopyLicense_Click"/>
                <Button Grid.Column="4"
                        Content="Generar licencia"
                        Height="46"
                        Click="BtnGenerateLicense_Click"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
